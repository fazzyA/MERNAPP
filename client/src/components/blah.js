import React from 'react'

function blah() {
    return (
        <div>
     Post - create, view list(sort), delete, single post
            1. apis
            read(get):  /api/v1/posts
            create(post):  /api/v1/posts/add
           single view(get): /api/v1/posts/:id
           delete(delete): /api/v1/posts/:id
           update(post): /api/v1/posts/:id

           
        </div>
    )
}

export default blah
